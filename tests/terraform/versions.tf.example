terraform {
  required_version = ">= 0.13"
  # backend "s3" {
  #   bucket  = "<bucket>"
  #   region  = "us-east-1"
  #   key     = "<path>/terraform.tfstate"
  #   encrypt = true
  # }
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 3.0"
    }
    github = {
      source  = "integrations/github"
      version = "~> 4.0"
    }
  }
}

provider "aws" {
  region = "us-east-1"
}

provider "github" {}
